{
  "id": "gitleaks",
  "version": "1.0.2",
  "name": "Gitleaks (via Github Releases)",
  "documentationURL": "http://github.com/aps831/devcontainers-features/tree/master/src/gitleaks",
  "description": "Gitleaks is a SAST tool for detecting and preventing hardcoded secrets like passwords, api keys, and tokens in git repos.",
  "options": {
    "version": {
      "type": "string",
      "proposals": ["latest"],
      "default": "latest",
      "description": "Select the version of Gitleaks to install."
    }
  },
  "installsAfter": ["ghcr.io/devcontainers-extra/features/gh-release"]
}
